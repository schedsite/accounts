pipeline:
  test:
    image: "node:9"
    environment:
      NODE_ENV: development
    commands:
      - ./install_phantomjs.sh
      - npm config set loglevel warn
      - npm set audit false
      - npm i
      - ng test sched-accounts-app --no-progress
      - ng test accounts --no-progress
